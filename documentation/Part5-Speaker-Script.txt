PART 5 - SPEAKER SCRIPT: LOCAL WALKTHROUGH AND CHECKPOINT EVIDENCE
Junior DevOps Engineer Exam 2026
Mark Andrei Castillo

Presentation rule: local action -> captured evidence -> checkpoint result.

SLIDE 1: Opening

SAY:
I will walk through the work the way I actually performed it: from the local terminal, to the captured screenshot, to the checkpoint that records the result. I will not treat a design diagram as proof by itself.

SHOW / DO:
Start with the PDF, then say that the screenshots are the evidence trail.

EVIDENCE NOTE:
The execution boundary matters: Parts 3 and 4 were run on my local WSL machine, while this repository contains the code and evidence.

------------------------------------------------------------------------

SLIDE 2: Evidence path

SAY:
Every part follows the same pattern. I ran something locally, saved the output, and then used the checkpoint to decide what can be claimed as complete. I also separate completed work from future improvements.

SHOW / DO:
Point to the four boxes: local WSL, capture, checkpoint, explanation.

EVIDENCE NOTE:
This prevents me from claiming that a planned scan or rollback test was already performed.

------------------------------------------------------------------------

SLIDE 3: Part 1 system health

SAY:
For Part 1, I began with the Linux environment on WSL2. I ran the system health script and captured the report with uptime, memory, disk, processes, and CPU information.

SHOW / DO:
Zoom into the command at the top and the END OF REPORT marker.

EVIDENCE NOTE:
The checkpoint identifies the machine as Aloof, Ubuntu 24.04 on WSL2.

------------------------------------------------------------------------

SLIDE 4: Part 1 scripts

SAY:
I then ran the backup and log-analysis scripts. The backup produced a timestamped archive, while the log script counted four errors and two warnings and printed the latest entries. This made the Linux work repeatable instead of only manual commands.

SHOW / DO:
Show the backup filename and the ERROR/WARNING counts.

EVIDENCE NOTE:
Part 1 is recorded as fully complete with four scripts and eleven screenshots.

------------------------------------------------------------------------

SLIDE 5: Part 2 source

SAY:
Before building containers, I checked that the actual API and UI source repositories were present. This was important because the first container attempt did not contain the real application source.

SHOW / DO:
Point at api-src/main.py, requirements.txt, ui-src/app, and package.json in the screenshot.

EVIDENCE NOTE:
The final Compose build contexts are api-src and ui-src, not empty wrapper folders.

------------------------------------------------------------------------

SLIDE 6: Part 2 correction

SAY:
The first Compose run failed because the API image did not have the MySQL Python driver. I corrected the dependency and aligned the database setup with the cloned application. The later screenshot shows all three services healthy.

SHOW / DO:
Compare the ModuleNotFoundError screenshot with the final docker-compose ps output.

EVIDENCE NOTE:
The legacy docker-compose command was used locally because that is what the WSL environment provided.

------------------------------------------------------------------------

SLIDE 7: Part 2 response

SAY:
After the containers were healthy, I tested the API root and the trip endpoint, and I checked the UI. The checkpoint records the final API, MySQL, and UI state and the successful test trip.

SHOW / DO:
Show the browser response, then read the curl checks on the slide.

EVIDENCE NOTE:
This is the difference between an image that builds and an application stack that actually responds.

------------------------------------------------------------------------

SLIDE 8: Part 3 push

SAY:
For Part 3, I pushed the staging branch from the local WSL checkout. That push is the handoff point: the local Git result is followed by the GitHub Actions run history.

SHOW / DO:
Read the `git push origin staging` output and then point to the green staging runs.

EVIDENCE NOTE:
The checkpoint says the workflow was created and executed on the candidate's personal WSL computer, with GitHub as the remote runner.

------------------------------------------------------------------------

SLIDE 9: Part 3 workflow

SAY:
The successful workflow graph shows the actual order: Verify, Build Docker images, Test applications, and Push images to Docker Hub. The checkpoint records a separate staging runtime and historical successful email evidence. The current workflow also contains a Discord notification job, but its delivery is not captured here.

SHOW / DO:
Trace the four green jobs from left to right.

EVIDENCE NOTE:
Node deprecation annotations are warnings, not failed jobs. Do not mix the historical email screenshot with the current Discord configuration. The optional image scan, failure-notification delivery, and live rollback remain follow-up work.

------------------------------------------------------------------------

SLIDE 10: Part 4 deployment

SAY:
For Part 4, I moved to Kubernetes locally. The k9s capture shows two API pods, two UI pods, and one database pod running, with the application replicas placed across the two Minikube nodes.

SHOW / DO:
Point to READY 1/1, zero restarts, the pod names, and the node column.

EVIDENCE NOTE:
The checkpoint records the two-node cluster, Kubernetes versions, enabled Metrics Server, and Ingress controller.

------------------------------------------------------------------------

SLIDE 11: Part 4 runtime tests

SAY:
I did not stop at a successful rollout. The verifier sent 40 requests and recorded 20 responses from each API replica. Then it stopped one Minikube node and still received 20 out of 20 HTTP 200 responses before restoring the node.

SHOW / DO:
Read the successful requests and node-failure lines directly from the screenshots.

EVIDENCE NOTE:
These are the strongest runtime claims because they come from captured local WSL evidence, not only YAML.

------------------------------------------------------------------------

SLIDE 12: Closeout

SAY:
The checkpoint lets me make precise claims. Parts 1 and 2 have captured core evidence. Part 3 core CI/CD and registry publishing are evidenced, with a separate staging runtime capture. Part 4 runtime evidence is captured. Security scanning, failure-notification delivery, and live rollback remain unverified.

SHOW / DO:
Finish by opening CHECKPOINT.md and the documentation folders.

EVIDENCE NOTE:
The final walkthrough is run locally, captured, reconciled against the checkpoint, and explained with the correct evidence boundary.

------------------------------------------------------------------------
