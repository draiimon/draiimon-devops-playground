apiVersion: v1
kind: Service
metadata:
  name: ui-service
  namespace: devops-exam
  labels:
    app: ui-app
spec:
  type: ClusterIP
  selector:
    app: ui-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
